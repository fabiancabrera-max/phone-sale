# Arquitectura Backend - Phone Sale

## Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│               SERVER COMPONENTS (Next.js App Router)            │
│  ┌──────────────────┐  ┌──────────────────┐                     │
│  │   Home Page      │  │  Product Detail  │                     │
│  │ (Direct DB Acc)  │  │ (Direct DB Acc)  │                     │
│  └────────┬─────────┘  └────────┬─────────┘                     │
│           │                     │                               │
│           ▼                     ▼                               │
│      ┌───────────────────────────────────┐                      │
│      │        Backend Lib (SSR)          │                      │
│      │     (src/backend/lib/products)    │                      │
│      └────────────────┬──────────────────┘                      │
│                       │                                         │
└───────────────┬───────┼─────────────────────────────────────────┘
                │       │
                ▼       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API ROUTES (Client Interactivity)             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ POST /api/products/upload-image                          │   │
│  │  - Recibe productId del frontend (generated client-side) │   │
│  │  - Valida token y cuotas                                 │   │
│  │  - Sube a Storage: images/{uuid}.{ext}                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ POST /api/products (Create)                              │   │
│  │  - Recibe ID pre-generado                                │   │
│  │  - Crea documento en Firestore                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ DELETE /api/products/[id]                                │   │
│  │  - Borra producto y metadata                             │   │
│  │  - Borra imágenes físicas en Storage                     │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────┬───────────────────────┬───────────────────────────────┘
          │                       │
          ▼                       ▼
┌──────────────────┐    ┌──────────────────────┐
│  Cloud Storage   │    │     Firestore        │
│  ┌────────────┐  │    │  ┌────────────────┐  │
│  │   images/  │  │    │  │   products/    │  │
│  │   {uuid}   │  │    │  │   - id         │  │
│  │   .jpg     │  │    │  │   - status     │  │
│  └────────────┘  │    │  │   - specs      │  │
│                  │    │  └────────────────┘  │
│                  │    │  ┌────────────────┐  │
│                  │    │  │  image_quota/  │  │
│                  │    │  └────────────────┘  │
└──────────────────┘    └──────────────────────┘
```

## Flujo de Creación de Producto (Robustez)

1.  **Frontend (ProductForm)**:
    - Genera un `UUID` único para el producto (`tempId`) si es nuevo.
    - Este ID se mantiene constante durante la sesión de edición.
2.  **Upload de Imágenes**:
    - El usuario sube imágenes.
    - Se envía el `productId` (`tempId`) al endpoint de upload.
    - Las imágenes se guardan vinculadas a ese ID desde el minuto cero.
3.  **Guardado del Producto**:
    - Al hacer submit, se envía el producto completo **con el mismo ID** al backend.
    - El backend crea el documento usando ese ID específico.
    - **Resultado**: Integridad referencial garantizada. No hay imágenes huérfanas.

## Estructura de Proyecto (Key Locations)

- `src/backend/lib/`: Lógica de negocio y acceso a datos (usada por API y Server Components).
- `src/app/api/`: Endpoints REST para operaciones de escritura/admin desde el cliente.
- `src/app/(public)`: `page.tsx`, `product/[id]/page.tsx` (Server Components).
  - **Nota**: Rutas dinámicas usan `await params` (Next.js 15+ standard).
- `src/app/admin/_components/`: Componentes privados del panel de administración (ej: `ProductForm`).
- **Nota**: No existen datos mock (`src/frontend/data` eliminado). Toda la data es real.

## Estructura de Datos (Firestore)

### Collection `products`

```typescript
{
  id: string;                    // UUID (Generated by Frontend or Backend)
  title: string;                 // "iPhone 15 Pro Max"
  description: string;           // Detalle
  price: number;                 // 1299
  specs: {
    condition: 'new' | 'used_as_new' | 'used' | 'refurbished',
    color: string,
    storage: string,
  };
  images: string[];              // URLs públicas
  status: 'featured' | 'on_sale' | 'sold';
  createdAt: Timestamp;
  updatedAt: Timestamp;
  createdBy: string;             // UID del admin
}
```

## Seguridad y Control de Costos

### Índices de Firestore (Requeridos)

Para permitir el ordenamiento y filtrado eficiente, se requieren los siguientes índices compuestos (ya configurados):

1.  Collection `products`: `status` (Asc) + `createdAt` (Desc)
2.  Collection `products`: `createdBy` (Asc) + `createdAt` (Desc)

### Validaciones Backend

- Tipado fuerte en `src/backend/lib/products.ts` usando `Query` y `Record<string, any>` para evitar `any`.
- Sanitización de inputs en API Routes.

## Checklist de Implementación

### Fase 1: Setup ✅

- [x] Firebase Project & Auth
- [x] Firestore & Storage Rules
- [x] Environment Variables

### Fase 2: Backend Core ✅

- [x] Firebase Admin Config
- [x] Auth Middleware
- [x] CRUD Operations (Strongly Typed)
- [x] Quota Tracking

### Fase 3: Frontend Admin ✅

- [x] Dashboard Layout
- [x] Product Form (Moved to `src/app/admin/_components`)
- [x] Image Upload con Preview
- [x] Edición y Borrado (incluyendo limpieza de imágenes)

### Fase 4: Frontend Public (SSR) ✅

- [x] Home Page (Server Component)
- [x] Product Detail (Server Component)
- [x] Galerías y Componentes UI Optimizados (Chips, Cards)
- [x] Integración de Datos Reales
- [x] Índices creados y ordenamiento activo

### Pendiente

- [ ] Optimización de imágenes (Next.js Image / Firebase Resize).
